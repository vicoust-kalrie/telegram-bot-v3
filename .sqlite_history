CREATE TABLE portfolio (
coin TEXT,
jumlah REAL
);
INSERT INTO portfolio VALUES ('SOL', 1);
INSERT INTO portfolio VALUES ('XRP', 25);
INSERT INTO portfolio VALUES ('ETH', 0.005);
SELECT * FROM portfolio;
.exit
.schema
.exit 
CREATE TABLE history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tanggal TEXT,
    aksi TEXT,
    coin TEXT,
    jumlah REAL
);
.tables
.exit
CREATE TABLE portfolio_new (
    user_id INTEGER,
    coin TEXT,
    jumlah REAL
);
INSERT INTO portfolio_new
SELECT 8728106142, coin, jumlah
FROM portfolio;
SELECT * FROM portfolio_new;
SELECT COUNT(*) FROM portfolio;
SELECT COUNT(*) FROM portfolio_new;
ALTER TABLE portfolio RENAME TO portfolio_old;
ALTER TABLE portfolio_new RENAME TO portfolio;
.tables
SELECT * FROM portfolio;
.quit
